package com.partygameonline.game.liarsnumber.domain;

public enum LiarsNumberPhase {
    SELECT_CARD,
    SELECT_TARGET,
    DECLARE_TYPE,
    RECEIVER_DECISION,
    SELECT_PASS_TARGET,
    PASS_DECLARE_TYPE,
    GAME_OVER
}
