package com.partygameonline.game.liarsnumber.domain;

public enum LiarsNumberActionType {
    SELECT_CARD,
    SELECT_TARGET,
    DECLARE_TYPE,
    GUESS,
    PEEK_AND_PASS,
    SELECT_PASS_TARGET,
    PASS_DECLARE_TYPE,
    TIMEOUT
}
