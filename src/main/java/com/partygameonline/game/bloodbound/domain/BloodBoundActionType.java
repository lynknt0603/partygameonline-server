package com.partygameonline.game.bloodbound.domain;

public enum BloodBoundActionType {
    LOOK_LEFT_ACK,
    ATTACK,
    INTERVENE,
    PASS_INTERVENTION,
    REVEAL_WOUND_TOKEN,
    USE_ABILITY,
    PASS_DAGGER,
    CAPTURE,
    TIMEOUT
}
